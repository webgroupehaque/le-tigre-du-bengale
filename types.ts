// Types propres au site. La carte, le panier et les réglages viennent du kit (@kit).
export type { MenuItem, MenuOption, MenuChoice, OrderType, CartLine } from '@kit';

export type PageView = 'home' | 'order' | 'contact' | 'legal';
